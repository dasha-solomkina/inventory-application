<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./styles.css" />
    <title>Document</title>
  </head>
  <body>
    <header><h1><%= title %></h1></header>

    <nav>
      <a href="/" id="go-home">go home</a>
      <a href="/new">
        <button>new creature</button>
      </a>
    </nav>

    <div class="grid-cards">
      <% creatures.forEach((creature) => { %>
      <div class="card creature">
        <img src="<%= creature.img %>" alt="<%= creature.name %>" />
        <h2><%= creature.name %></h2>
        <p class="characteristic" data-unit="class">
          <%= creature.class_name %>
        </p>
        <p class="characteristic" data-unit="km/h"><%= creature.speed %></p>
        <p class="characteristic" data-unit="kg"><%= creature.max_weight %></p>
        <p class="characteristic" data-unit="years"><%= creature.lifespan %></p>
        <form
          action="/creature/<%= creature.id %>/delete"
          method="POST"
          style="display: inline"
          class="delete"
        >
          <button
            type="submit"
            onclick="return confirm('Are you sure you want to delete this creature?');"
          >
            delete
          </button>
        </form>
      </div>
      <% }) %>
    </div>
  </body>
</html>
